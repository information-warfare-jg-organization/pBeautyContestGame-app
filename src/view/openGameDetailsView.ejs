<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Gra #<%= gameId %> • Otwarta</title>
  <link rel="stylesheet" href="/style/style.css" />
  <style>
    .center-wrap {
      min-height: calc(100dvh - 80px);
      display: grid;
      place-items: center;
      text-align: center;
    }
    .headline { font-size: 24px; font-weight: 700; margin-bottom: 12px; }
    .subline { font-size: 16px; color: #555; margin-bottom: 20px; }
    .btn-primary {
      display:inline-block; padding: 12px 28px; border-radius: 10px;
      background:#0f62fe; color:#fff; text-decoration:none; font-weight:600;
      border:1px solid #0f62fe; text-transform: uppercase; letter-spacing:.5px;
    }
  </style>
</head>
<body>
  <div class="center-wrap">
    <div>
      <div class="headline">Gra jest otwarta, możesz dodać swoją odpowiedź!</div>
      <div class="subline">Aktualna liczba udzielonych odpowiedzi: <strong><%= answersCount %></strong></div>
      <a class="btn-primary" href="/games/<%= gameId %>/answer">Dodaj kolejną odpowiedź</a>
    </div>
  </div>
</body>
</html>
